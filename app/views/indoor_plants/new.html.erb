<h1>Create Indoor Plants</h1>

<%= form_for(@indoor_plant) do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name %>
    <br><br>

    <%= f.label :price %>
    <%= f.text_field :price %>
    <br><br>

    <%= f.label :store_id, "Select a Store" %>
    <%= f.collection_select :store_id, Store.all, :id, :name, prompt: true %>
    
    <%= f.submit "Submit" %>

<% end %>